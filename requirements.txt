# Core Python dependencies
requests>=2.32.0
python-dotenv>=1.0.1
google-cloud-bigquery>=3.27.0

# dbt dependencies
# Use dbt-core and the BigQuery adapter together for the transformation layer.
dbt-core>=1.8.0,<2.0
dbt-bigquery>=1.8.0,<2.0

# Airflow orchestration
apache-airflow>=2.10.0,<3.0
apache-airflow-providers-google>=10.0.0

# Development and validation
pytest>=8.0.0
ruff>=0.6.0
